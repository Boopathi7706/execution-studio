/**
 * Web Worker placeholder for running physics-based D3 graph layouts.
 */
self.onmessage = (_event: MessageEvent) => {
  // Graph layout physics calculation stub
  self.postMessage({ positions: {} })
}
