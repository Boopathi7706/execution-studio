package com.executionstudio.visualization.model;

/**
 * Declares the semantic reason for highlighting a visual element.
 */
public enum HighlightReason {
    CURRENT,
    CHANGED,
    CREATED,
    REMOVED,
    SELECTED
}
