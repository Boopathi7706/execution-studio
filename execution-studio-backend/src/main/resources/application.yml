server:
  port: 8080

spring:
  application:
    name: execution-studio-backend
  main:
    banner-mode: "off"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
